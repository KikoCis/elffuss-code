// La batería vive en web/js/swe-tasks.js para que la use TAMBIÉN el navegador
// (Elffuss Lab la corre en vivo). Aquí solo se reexporta para el arnés de node.
export { TASKS } from '../../web/js/swe-tasks.js';
